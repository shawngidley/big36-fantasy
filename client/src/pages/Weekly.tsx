import { CalendarDays } from "lucide-react";
import { useState } from "react";
import LeagueShell from "@/components/LeagueShell";
import { EmptyLedger, LeagueError, LeagueLoading } from "@/components/LeagueState";
import { Badge } from "@/components/ui/badge";
import { trpc } from "@/lib/trpc";

export default function Weekly() {
  const [selectedWeekId, setSelectedWeekId] = useState<number | null>(null);
  const league = trpc.league.snapshot.useQuery();
  if (league.isLoading) return <LeagueShell><LeagueLoading /></LeagueShell>;
  if (league.error || !league.data) return <LeagueShell><LeagueError message={league.error?.message} /></LeagueShell>;
  const summary = league.data.weeklySummaries.find(item => item.id === (selectedWeekId ?? league.data.weeks[0]?.id));
  return <LeagueShell eyebrow="Week-by-week results"><section className="container pt-10 sm:pt-14"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="section-kicker">Scoreboard</p><h1 className="display-title mt-3">Weekly scoring</h1><p className="mt-3 max-w-xl text-sm leading-6 text-muted-foreground">A complete snapshot of every team’s weekly total from the commissioner’s event ledger.</p></div>{league.data.weeks.length ? <label className="flex items-center gap-2 rounded-lg border border-border bg-card px-3 py-2 text-sm font-semibold shadow-sm"><CalendarDays className="h-4 w-4 text-primary" /><span className="sr-only">Choose week</span><select className="bg-transparent outline-none" value={selectedWeekId ?? league.data.weeks[0]?.id} onChange={event => setSelectedWeekId(Number(event.target.value))}>{league.data.weeks.map(week => <option key={week.id} value={week.id}>Week {week.weekNumber} · {week.label}</option>)}</select></label> : null}</div>
    {summary ? <><div className="mt-9 overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="flex items-center justify-between border-b border-border bg-accent/40 px-5 py-4"><div><p className="font-display text-xl font-extrabold tracking-tight">Week {summary.weekNumber}</p><p className="mt-0.5 text-xs text-muted-foreground">{summary.label}</p></div><Badge variant={summary.status === "FINAL" ? "default" : "secondary"}>{summary.status.toLowerCase()}</Badge></div><div className="divide-y divide-border/70">{summary.teams.map((team, index) => <div key={team.ownerId} className="grid grid-cols-[38px_minmax(0,1fr)_auto] items-center gap-3 px-5 py-4"><span className="text-xs font-extrabold text-muted-foreground">{index + 1}</span><span className="truncate font-bold">{team.teamName}</span><span className="font-display text-lg font-extrabold tabular-nums">{team.points.toFixed(2)}</span></div>)}</div></div><section className="mt-8 overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="border-b border-border bg-accent/40 px-5 py-4"><p className="section-kicker">Official event ledger</p><h2 className="mt-1 font-display text-xl font-extrabold tracking-tight">Live scoring feed</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Every point change and subsequent correction is shown here as the official source ledger updates.</p></div>{league.data.events.filter(event => event.weekId === summary.id).length ? <div className="divide-y divide-border/70">{league.data.events.filter(event => event.weekId === summary.id).map(event => <div key={event.id} className="grid grid-cols-[minmax(0,1fr)_auto] gap-4 px-5 py-4"><div><div className="flex flex-wrap items-center gap-2"><Badge variant={event.auditAction === "CORRECTION" || event.auditAction === "REVERSAL" ? "secondary" : "default"}>{event.auditAction.toLowerCase()}</Badge><span className="text-xs font-bold text-primary">{event.positionLabel} · {event.schoolName}</span></div><p className="mt-2 text-sm font-semibold">{event.eventType.replaceAll("_", " ")}</p><p className="mt-1 text-xs text-muted-foreground">{event.note ?? "Official scoring event"} · {event.teamName}: {event.pointsBefore.toFixed(2)} → {event.pointsAfter.toFixed(2)} points · overall #{event.overallRankBefore} → #{event.overallRankAfter} · {new Date(event.createdAt).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })}</p></div><div className="text-right"><p className="font-display text-xl font-extrabold tabular-nums">{event.computedPoints > 0 ? "+" : ""}{event.computedPoints.toFixed(2)}</p><p className="mt-1 text-[10px] font-bold uppercase tracking-wider text-muted-foreground">points</p></div></div>)}</div> : <div className="p-8 text-sm leading-6 text-muted-foreground">The live event feed will appear as official scoring events are imported for this week.</div>}</section></> : <div className="mt-10"><EmptyLedger title="No scoring week is open" detail="The commissioner can create each week before entering the event-level scoring ledger." /></div>}</section></LeagueShell>;
}
