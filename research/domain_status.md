# 36football.com domain status

Checked on 2026-08-18. `36football.com` currently resolves to `192.64.119.206`. The `www` hostname resolves to `parking.d.parity.domains` (including `2.59.170.19` and `104.219.250.36`). An HTTPS request to `https://36football.com` timed out during TLS negotiation, and a browser navigation returned `ERR_CONNECTION_CLOSED`.

This indicates the custom domain is not yet assigned to the deployed 36 Football project. The current application domain remains `big36fantasy-fj5htb4x.manus.space`.

## Verification after assignment

Rechecked on 2026-08-18 after the project Domains panel added both hostnames. `https://36football.com` returns the deployed 36 Football application over HTTPS. `https://www.36football.com` returns a permanent redirect to the canonical `https://36football.com/`, which also renders the full public application. The custom domain setup is complete.
